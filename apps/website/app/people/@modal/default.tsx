/**
 * Nothing, which is what the popup slot holds whenever the URL is not a
 * member's profile -- on `/people` itself, and on a hard load of
 * `/people/<slug>`, where the standalone page renders instead.
 */
export default function ModalDefault() {
  return null;
}
