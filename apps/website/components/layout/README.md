# components/layout

The page skeleton and the primitives that control spacing: `Container`,
`Section`, site header, site footer, navigation.

Nothing here renders page-specific copy beyond navigation labels.

See ../../docs/COMPONENTS.md.
